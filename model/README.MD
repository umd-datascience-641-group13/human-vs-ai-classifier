# Model

*IMPORTANT*

FIRST Run `ai_human_preprocess.py` in `human-vs-ai-classifier/preprocessing` to create the cleaned csv data.

Make sure the following required file is in the current `/model` directory. 

Required file(s):
- `ai_human_clean.csv`
